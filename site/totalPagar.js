function totalPagar (gramas) {
    const a = gramas * 3.50
    if (total => 1000) {
        total - 0.50
    };

    else if (total <= 0) {
        total = 'Peso inválido'
    }
}
return(
    <main>
        <div clasname="queroumsovetinho">
            <h1></h1>
        </div>
    </main>
)